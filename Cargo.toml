[package]
name = "deepspace-game"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18"
noise = "0.9"

# Bevy is unusable in debug without this
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

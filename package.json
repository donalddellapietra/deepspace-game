{
  "name": "deepspace-game",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "trunk build --release --public-url \"./\"",
    "dev": "trunk serve",
    "deploy": "./deploy.sh"
  }
}
